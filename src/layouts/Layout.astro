---
import Footer from "../components/Footer.astro"
import HeadMeta from "../components/common/HeadMeta.astro"
import Header from "../components/Header.astro"
import "../styles/global.css"
import { siteConfig } from "../config"

type Meta = {
  title: string
  description: string
  url: string
  keywords?: string[]
  image?: string
  type?: "website" | "article"
}

type Props = {
  meta: Meta
}

const { meta } = Astro.props
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.bunny.net" />
    <link rel="alternate" type="application/rss+xml" title={`${siteConfig.personal.name}'s Blog`} href={`${siteConfig.site.baseUrl}/rss.xml`} />
    <link
      href="https://fonts.bunny.net/css?family=ibm-plex-mono:400,400i,500,500i,600,600i,700,700i"
      rel="stylesheet"
    />

    <!-- Theme detection script - runs before page renders -->
    <script is:inline>
      const theme = (() => {
        if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
          return localStorage.getItem("theme") ?? "light";
        }
        if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
          return "dark";
        }
        return "light";
      })();

      if (theme === "light") {
        document.documentElement.classList.remove("dark");
      } else {
        document.documentElement.classList.add("dark");
      }

      window.localStorage.setItem("theme", theme);
    </script>

    <!-- Umami analytics - disabled in dev, disabled on /expenses when authenticated -->
    {import.meta.env.PROD && (
      <script is:inline data-umami-src={siteConfig.analytics.umami.src} data-umami-id={siteConfig.analytics.umami.websiteId}>
        const skipAnalytics =
          location.pathname === "/expenses" &&
          !!localStorage.getItem("expenses-password");
        if (!skipAnalytics) {
          const s = document.createElement("script");
          s.defer = true;
          s.src = document.currentScript.dataset.umamiSrc;
          s.dataset.websiteId = document.currentScript.dataset.umamiId;
          document.head.appendChild(s);
        }
      </script>
    )}

    <HeadMeta {...meta} />
  </head>
  <body class="zag-bg zag-text zag-transition font-mono">
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>
