name: Deploy

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/deploy
        with:
          netlify-build-hook: ${{ secrets.NETLIFY_BUILD_HOOK }}
